import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.somnia.health',
  appName: 'Somnia AI Health',
  webDir: 'dist/client',
  bundledWebRuntime: false
};

export default config;